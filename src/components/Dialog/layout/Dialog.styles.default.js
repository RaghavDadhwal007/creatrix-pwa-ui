const styles = ((theme) => ({
    dialogTitleWrapper: {
        '& .MuiTypography-root': {
            color: theme.palette.cardColors.bgColor,
            textAlign: 'center',
        },
    },
}));

export default styles;
