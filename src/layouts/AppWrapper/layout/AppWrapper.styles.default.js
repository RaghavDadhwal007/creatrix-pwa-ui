const styles = (() => ({
    appwrapper_container: {
        display: 'flex',
        alignItems: 'center',
        screenX: 'hidden',
        justifyContent: 'center',
    },
}));

export default styles;
