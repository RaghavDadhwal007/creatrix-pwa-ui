const styles = ((theme) => ({
    subscribeBtn: {
        paddingTop: '35px',
        textAlign: 'center',
        '& .MuiButtonBase-root.tertiary-btn-1': {
            backgroundColor: theme.palette.greyDark,
        },
    },
}));

export default styles;
