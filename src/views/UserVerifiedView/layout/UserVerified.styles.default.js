const styles = (() => ({
    main: {
        height: '50vh',
        width: '100%',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        flexDirection: 'column',
    },
    heading: {
        paddingBottom: '40px',
    },
}));

export default styles;
