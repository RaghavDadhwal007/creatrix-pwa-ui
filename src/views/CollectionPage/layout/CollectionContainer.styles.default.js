const styles = ((theme) => ({
    mainCollectionContainer: {
        maxWidth: '1440px',
        width: '100%',
        margin: '7em auto 1em auto',
        color: theme.palette.white,
    },
}));

export default styles;
